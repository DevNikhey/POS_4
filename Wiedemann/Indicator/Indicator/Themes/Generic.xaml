<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Indicator">
    <Style TargetType="{x:Type local:CustomControl1}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:CustomControl1}">
                    <Viewbox>
                        <Grid>
                            <!-- Hintergrund -->
                            <Image Source="pack://application:,,,/Indicator;component/Resources/meter.png"/>


                            <!-- Zeiger -->
                            <Image Source="pack://application:,,,/Indicator;component/Resources/pointer.png" RenderTransformOrigin="0.5,0.5">
                                <Image.RenderTransform>
                                    <RotateTransform Angle="{Binding Angle, RelativeSource={RelativeSource TemplatedParent}}" />
                                </Image.RenderTransform>
                            </Image>

                            <!-- Werte -->
                            <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center">
                                <TextBlock Text="{Binding Minimum, RelativeSource={RelativeSource TemplatedParent}}"/>
                                <TextBlock Text="{Binding Value, RelativeSource={RelativeSource TemplatedParent}}"/>
                                <TextBlock Text="{Binding Maximum, RelativeSource={RelativeSource TemplatedParent}}"/>
                            </StackPanel>

                        </Grid>
                    </Viewbox>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
