<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="Primzahlgenerator.MainWindow"
        Title="Primzahlgenerator" Height="450" Width="800">

    <Window.Resources>
        <Storyboard x:Key="loadingRotation">
            <DoubleAnimation 
                Storyboard.TargetName="image1" 
                Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)" 
                From="0" To="360" 
                Duration="0:0:2"
                RepeatBehavior="Forever"/>
        </Storyboard>
    </Window.Resources>

    <Grid>
        <Image x:Name="image1" Width="124" Height="124" 
               Source="pack://application:,,,/imageLoading.png"
               HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image.RenderTransform>
                <RotateTransform Angle="0" CenterX="62" CenterY="62"/>
            </Image.RenderTransform>
        </Image>
        <ListView x:Name="ergNumbers" Width="200" Margin="0,110,600,0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Content="Button" Click="Button_Click" HorizontalAlignment="Left" Margin="52,72,0,0" VerticalAlignment="Top"/>
        <TextBox x:Name="input" HorizontalAlignment="Left" Margin="12,49,0,0" TextWrapping="Wrap" Text="5" VerticalAlignment="Top" Width="120"/>
        <Label Content="Find the xth:" HorizontalAlignment="Left" Margin="10,23,0,0" VerticalAlignment="Top"/>
    </Grid>
</Window>
