<Window x:Class="CountdownWeckerLibrary.DateTimeDlg"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ex="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:local="clr-namespace:CountdownWeckerLibrary"
        mc:Ignorable="d"
        Title="DateTimeDlg" Height="300" Width="400" ResizeMode="NoResize">
    <StackPanel Height="200">
        <Label Content="Minuten:"></Label>
        <TextBox x:Name="minutesBox"></TextBox>

        <Label Content="Sekunden:"></Label>
        <TextBox x:Name="secondsBox"></TextBox>
        <Grid Height="100">
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition ></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="60"></RowDefinition>
            </Grid.RowDefinitions>
            <Button Padding="10" Margin="10" Click="Button_Click">Cancel</Button>
            <Button Padding="10" Margin="10" x:Name="buttonOkay" Grid.Column="1" Click="buttonOkay_Click">OK</Button>
        </Grid>
    </StackPanel>
</Window>