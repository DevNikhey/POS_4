<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:CountdownWeckerLibrary">

    <Style TargetType="{x:Type local:Wecker}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Wecker}">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">

                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">

                            <TextBlock
                                x:Name="PART_REMAININGTIME"
                                FontSize="48"
                                Foreground="Red"
                                HorizontalAlignment="Center"
                                TextAlignment="Center" />

                            <CheckBox x:Name="PART_CHECKBOXALARMSET"
                                      Content="Countdown Active"
                                      Margin="0,10,0,10"
                                      HorizontalAlignment="Center" />

                            <Button x:Name="PART_SETALARMBUTTON" Padding="10" Margin="0,10,0,0">
                                <StackPanel>
                                    <TextBlock FontSize="20" Text="Set Countdown" HorizontalAlignment="Center" />
                                    <TextBlock FontSize="20"
                                               x:Name="PART_SETALARMBUTTONTEXTPANE"
                                               HorizontalAlignment="Center"/>
                                </StackPanel>
                            </Button>

                        </StackPanel>

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
